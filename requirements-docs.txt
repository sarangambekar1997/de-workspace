# Documentation site build (MkDocs + Material theme)
# mkdocs is capped below 2.0: MkDocs 2.0 is incompatible with the Material theme's plugin/theming system
mkdocs>=1.6.1,<2
mkdocs-material==9.7.7
pymdown-extensions==12.1
